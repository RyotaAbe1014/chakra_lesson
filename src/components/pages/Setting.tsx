import { FC, memo } from "react";
import { Header } from "../organisms/layout/Header";
export const Setting: FC = memo(() => {
  return (
    <>
      <Header></Header>
      <p>setting</p>
    </>
  )
});